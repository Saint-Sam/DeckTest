# T1.12 Fuzz Harness Report

Date: 2026-07-07

## Scope

Implemented two cargo-fuzz targets:

- `fuzz_apply`: byte-driven kernel action sequences through `legal_actions`
  and `apply`, plus scripted setup/life/poison/SBA/view/hash probes.
- `fuzz_scenarioparse`: UTF-8 bounded scenario parser and runner fuzzing for
  the dependency-free RON-compatible testkit schema.

## Local Verification

PASS:

- `cargo fmt --all`
- `cargo fmt --manifest-path fuzz/Cargo.toml --all`
- `cargo fuzz list` discovered `fuzz_apply` and `fuzz_scenarioparse`
- `cargo fuzz check --sanitizer none`
- `cargo fuzz run --sanitizer none fuzz_apply -- -max_total_time=1`
- `cargo fuzz run --sanitizer none fuzz_scenarioparse -- -max_total_time=1`
- `FORGE_FUZZ_SANITIZER=none FORGE_FUZZ_SECONDS=1 scripts/fuzz_nightly.sh`
- `cargo +nightly fuzz check`
- `FORGE_FUZZ_SECONDS=1 scripts/fuzz_nightly.sh`
- `scripts/fuzz_nightly.sh --t1-gate`

Notes:

- Local machine initially had only stable Rust installed. Default sanitizer
  fuzzing failed as expected with `the option Z is only accepted on the nightly
  compiler`; nightly Rust was then installed and pinned in
  `docs/toolchain.lock.md`.
- `cargo fuzz check --sanitizer none` required approved crates.io egress to
  download `libfuzzer-sys` and `arbitrary`.
- The short smoke runs completed cleanly before the long gate.
- The 6-hour gate log is `reports/fuzz/T1.12-2026-07-07-run.log`.
- No `ERROR`, `CRASH`, `SUMMARY`, `panic`, `AddressSanitizer`, libFuzzer error,
  or crash artifact marker was found in the gate log.

## Six-Hour Gate Result

PASS:

- `fuzz_apply`: 6,530,854 runs in 10,801 seconds.
- `fuzz_scenarioparse`: 895,666,102 runs in 10,801 seconds.

## Six-Hour Gate Command

The T1.12 acceptance run is:

```bash
scripts/fuzz_nightly.sh --t1-gate
```

With the current two targets, `--t1-gate` runs each target for 10,800 seconds,
for a 6-hour aggregate fuzz run. Override `FORGE_FUZZ_SECONDS` only when a gate
review explicitly records a different interpretation.

For local stable-only smoke checks:

```bash
FORGE_FUZZ_SANITIZER=none FORGE_FUZZ_SECONDS=1 scripts/fuzz_nightly.sh
```

## Status

Fuzz harnesses are implemented, smoke-verified, and 6-hour gate verified.
