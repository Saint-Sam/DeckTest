# Remote CI Evidence - T0 Remediation

Date: 2026-07-06

Repository: `Saint-Sam/Forge-2.0`

Commit under test: `3ee6166a6a969207088c46beda5e7c29a914ad48`
(`T0.3: record remote CI remediation evidence`)

Workflow run: `ci #4`

Run URL:
`https://github.com/Saint-Sam/Forge-2.0/actions/runs/28816494698`

Result: PASS.

Required T0.3 jobs:

- `fmt`: success
- `clippy`: success
- `test-linux`: success
- `test-macos`: success
- `test-windows`: success
- `build-wasm`: success
- `build-android`: success
- `coverage`: success
- `deny-audit`: success
- `verification-loop`: success
- `determinism-replay`: success

Fixes validated by this run:

- `verification-loop` installs `cargo-llvm-cov` before running
  `scripts/vl.sh`.
- `cargo deny` now uses `deny.toml` and explicitly allows the project license
  `GPL-3.0-only`, matching O2.
- The T0 bundle-generation change that copies gate evidence into bundles is on
  `main` and passes remote CI.

Branch-protection note:

The T0.3 plan says branch protection should require the CI checks. This private
bootstrap repository was published and pushed by the Owner through GitHub
Desktop, then validated by a green required-equivalent remote CI run on `main`.
Branch protection was not changed automatically because that is a GitHub
permission/settings change. Before opening collaboration branches or making a
release, configure `main` protection to require the jobs listed above.
