# Weekly Status 2026-W28

Tier: T1 Rules kernel v1

Progress: pre-flight O1/O2 completed and T0 passed conditional. T1.1 core
state is verified locally and remotely; GitHub Actions `ci #6` run
`28818490265` passed all required jobs on commit `7f80433`. T1.2 turn
structure is verified locally and remotely; GitHub Actions `ci #7` run
`28819302498` passed all required jobs on commit `c151cb4`. T1.3 priority
and stack is verified locally and remotely; GitHub Actions `ci #8` run
`28820393577` passed all required jobs on commit `41fccc0`. T1.4 mana and
payment plans are verified locally and remotely; GitHub Actions `ci #9` run
`28821155679` passed all required jobs on commit `be950cc`. T1.5 casting
pipeline is verified locally and remotely; GitHub Actions `ci #10` run
`28822340130` passed all required jobs on commit `14ed220`. T1.6 combat is
verified locally and remotely; GitHub Actions `ci #11` run `28823362044`
passed all required jobs on commit `d0f9fd3`. T1.7 state-based actions are
verified locally and remotely; GitHub Actions `ci #12` run `28824832805`
passed all required jobs on commit `6491d5f`. T1.7 includes the CR 704.5 table,
repeat-to-fixpoint SBA loop, pre-priority checks, empty-library/life/poison
loss, zero-toughness, lethal damage, deathtouch damage, lifelink-before-loss,
cleanup-repeat behavior, and deterministic hashing of pending SBA state. T1.R1
is verified locally: the kernel now exposes public mutation through
`legal_actions`/`apply`, direct `GameState` mutators are private, and
`scripts/review/no_public_mutating_gamestate.sh` is wired into VL. T1.R2 is
verified locally: object records now store base creature characteristics, and
combat/SBA logic uses the derived `creature_characteristics` query path. T1.R3
is verified locally: `forge-core` now exposes `PlayerView` redaction for player
and AI consumers, hand/library identities are hidden through the public
projection, `forge-ai` accepts `PlayerView` rather than `GameState`, and the old
full-state snapshot helper has been removed from the consumer API. T1.R4 is
verified locally: clone-surface invariants are documented in `forge-core`,
`scripts/review/clone_surface_guard.sh` is wired into VL, and
`metrics/clone_surface.json` records an 18-field persistent allocation baseline
with no full-state snapshot surface and no `GameState` debug dump. CP-KERNEL
scoped re-review passed at `d7fcb03`. T1.8 is verified locally: `forge-core`
now supports deterministic seeded turn-order selection, opening-hand draw,
London mulligan redraw and keep/bottoming through the `Action`/`apply` surface,
setup-aware first-turn guarding, canonical hashing of setup/RNG state, and
PlayerView canaries that keep opponent hands and all libraries hidden after
opening hands are drawn. T1.9 is verified locally: `forge-testkit` now includes
a dependency-free RON-compatible scenario schema for `{setup, script, expect}`,
a public runner that exercises the kernel through `Action`/`apply`, invariant
checks for zone conservation, life/poison sanity, hash consistency, and hash
determinism, JUnit-style XML reporting, `lint`/`oracle` CLI commands, and a
smoke oracle scenario for opening-hand setup.

Metrics: `metrics/legacy_inventory.json` reports 33,290 legacy card scripts,
43,649 ability lines, and 251 distinct keyword rows. `forge-core` now has 70
unit tests for typed zones, hashing, turn advancement, cleanup timing, priority
rotation, stack ordering, LIFO resolution, mana pools, payment plans, and
auto-tap ordering, casting, target legality snapshots, fizzle handling, and
keyword-simple combat, plus T1.7 state-based-action coverage and T1.R1
action-surface coverage, T1.R2 base/derived characteristic coverage, and T1.R3
hidden-information projection coverage, plus T1.8 deterministic setup and
London mulligan coverage. `forge-testkit` now has 4 library unit tests, 8 CLI
unit tests, and 1 committed oracle scenario. `forge-ai` now has 2 unit tests,
including the `PlayerView` entrypoint canary. The latest local VL run ended
with `ALL CHECKS PASSED`; coverage was regenerated to `metrics/coverage.json`
and the oracle subset passed 1 scenario with 0 failures.

Open blockers/QIDs by priority: none in the Question Queue.

Checkpoint calendar: T0 Gate re-review returned PASS CONDITIONAL. CP-KERNEL
returned FAIL on 2026-07-06 at `6491d5f`, then passed scoped re-review on
2026-07-06 at `d7fcb03` after T1.R1-T1.R4 remediation. T1.8 completed local
verification on 2026-07-06. T1.9 completed local verification on 2026-07-07.
The next planned task is T1.10: port the top 100 legacy `forge-game` unit tests
into oracle scenarios with a committed legacy-test to scenario mapping table.
The next human-heavy checkpoint remains CP-LAYERS in Tier 2.

De-scope pressure: none. CP-KERNEL is a repair checkpoint, not a de-scope
request.

What I am least confident about this week: branch protection is not yet enabled
on GitHub `main`; it should be configured before collaboration branches or
release.

## WHAT YOU SHOULD EXPECT NEXT

The Orchestrator will continue into T1.10 legacy unit-test mining and oracle
scenario porting.

## WHAT WE NEED FROM YOU

No immediate owner input unless you want to configure branch protection now.
Before collaboration branches or release, protect `main` and require the T0.3
CI jobs.
