# T0.1 Install Request: Rust Toolchain

Date: 2026-07-06

`scripts/vl.sh` and `scripts/gates/gate_T0.sh` are blocked because this machine
does not currently expose `rustup`, `rustc`, or `cargo` on PATH.

Please run the following in a normal macOS Terminal window:

```bash
cd "/Users/juanlopez2016/Desktop/Forge 2.0"
bash scripts/bootstrap_toolchain.sh
```

When that finishes, tell Codex:

```text
Rust install finished.
```

The Orchestrator will then run:

```bash
scripts/check_toolchain.sh
scripts/vl.sh
scripts/gates/gate_T0.sh
```

If the installer asks for a choice, accept the standard/default installation.
For a GitHub ZIP download, use the same bootstrap command; it fetches the pinned
legacy Forge reference because ZIP archives do not include submodule contents.
