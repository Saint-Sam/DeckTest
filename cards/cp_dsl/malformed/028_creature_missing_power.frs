card "Test Card" {
  id: "test-card"
  layout: normal
  status: verified_playable
  face "Test Card" {
    cost: "{1}{G}"
    types: "Creature — Elf"
    oracle: "Flying. {T}: Draw a card."
    toughness: "2"
    keywords: [flying]
    ability activated {
      costs: [tap_self()]
      effect: draw(1, you())
    }
  }
}
